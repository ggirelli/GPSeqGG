TODO
===

* Optimize UMI deduplication memory usage.
* Optimize memory usage for linkers addition to SAM file.
* Optimize binning.

* Allow to remove any chromosomes, or instead specify which chromosomes to keep.
* The 'unmapped'reads in SAM filter step are actually not mapped to chr1-22,X,Y.
* Make pipeline not fixed for human genome (i.e., chr1-22,X,Y)

* Add visualization options to the bedfile header line.

* Implement global radiality centrality.
